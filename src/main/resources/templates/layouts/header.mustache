<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>게시판</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@700&display=swap" rel="stylesheet"> <!-- Google Fonts 추가 -->
    <style>
        body {
            background-color: #f8f9fa; /* 배경색 */
            font-family: 'Roboto', sans-serif; /* 폰트 설정 */
        }
        .navbar {
            border-bottom: 2px solid #4a90e2; /* 상단바 경계선 */
            margin-bottom: 20px; /* 상단바와 내용 간격 */
            width: 86%; /* 경계선 너비 조정 (86%로 설정) */
            left: 50%;
            transform: translateX(-50%); /* 중앙으로 이동 */
        }
        footer {
            border-top: 2px solid #4a90e2; /* 하단바 경계선 */
            color: #000000; /* 하단바 텍스트 색상 */
            padding: 20px 0; /* 상하 패딩 추가 */
            text-align: center;
            margin-top: 50px; /* 하단바와 내용 간격 */
            width: 86%; /* 경계선 너비 조정 (상단바와 동일하게 설정) */
            left: 50%;
            transform: translateX(-50%); /* 중앙으로 이동 */
            position: relative; /* 상대 위치 설정 */
        }
        .nav-link {
            color: #000000; /* 상단바 텍스트 색상 */
        }
        .nav-link:hover {
            color: #4a90e2; /* 호버 시 텍스트 색상 */
        }
        .post {
            background-color: #ffffff; /* 게시물 배경색 */
            border: 1px solid #dee2e6; /* 게시물 테두리 */
            border-radius: 5px; /* 모서리 둥글게 */
            padding: 15px; /* 패딩 */
            margin-bottom: 20px; /* 게시물 간격 */
        }
        .post-title {
            font-size: 1.8rem; /* 제목 크기 */
            margin-bottom: 10px; /* 제목과 본문 간격 */
            font-weight: bold; /* 제목 진하게 */
            color: #333333; /* 제목 색상 */
        }
        .post-meta {
            font-size: 0.9rem; /* 메타 정보 크기 */
            color: #6c757d; /* 메타 정보 색상 */
            margin-bottom: 10px; /* 메타 정보와 본문 간격 */
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">게시판</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="#">홈</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">글쓰기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">내 글</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    .navbar-nav .nav-link {
        font-weight: bold; /* 진하게 표시 */
        transition: color 0.3s; /* 색상 전환 효과 */
    }

    .navbar-nav .nav-link:hover {
        color: #007bff; /* 호버 시 강조 색상 */
        text-decoration: underline; /* 호버 시 밑줄 추가 */
    }

    .navbar-nav .nav-link.active {
        color: #0056b3; /* 활성화된 링크 색상 */
    }
</style>